apiVersion: v1
kind: Service
metadata:
  name: web-demo-service
  labels:
    app: web-demo
spec:
  type: NodePort
  selector:
    app: web-demo
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30085