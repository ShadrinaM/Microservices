apiVersion: v1
kind: Pod
metadata:
  name: problem-pod-non-existent
spec:
  containers:
  - name: test-container
    image: nginx:alpine
    env:
    - name: MISSING_SECRET_VALUE
      valueFrom:
        secretKeyRef:
          name: non-existent-secret  # Secret не существует
          key: some-key