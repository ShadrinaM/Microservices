apiVersion: v1
kind: ConfigMap
metadata:
  name: ui-config
  namespace: user-platform-exam
data:
  login.title: "{{ .Values.uiConfig.loginTitle }}"
  welcome.message: "{{ .Values.uiConfig.welcomeMessage }}"