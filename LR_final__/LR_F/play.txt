cd D:\Git\Microservices\LR_final__\LR_F

kubectl apply -f kubernetes/namespace.yaml
kubectl apply -f kubernetes/configmap-ui.yaml
kubectl apply -f kubernetes/secret.yaml
kubectl apply -f kubernetes/configmap-postgres.yaml

# 5. Разворачиваем PostgreSQL и ждем его готовности
kubectl apply -f kubernetes/postgres.yaml

# 6. Ждем, пока PostgreSQL запустится и инициализирует БД
kubectl wait --for=condition=ready pod/postgres-0 -n user-platform-exam --timeout=300s

# 7. Проверьте БД
kubectl exec -n user-platform-exam postgres-0 -- psql -U admin -d userdb -c "\d users"
kubectl exec -n user-platform-exam postgres-0 -- psql -U admin -d userdb -c "SELECT * FROM users;"

cd src/auth-service
docker build -t auth-service:latest .
docker images | findstr "auth-service"
cd ../..

# 8. Разворачиваем auth-service
kubectl apply -f kubernetes/auth-service.yaml

# 9. Проверяем статус всех компонентов
kubectl get all -n user-platform-exam

#===================================
# ТЕСТЫ auth-service
#===================================
# Получаем доступ к auth-service через port-forward
kubectl port-forward -n user-platform-exam service/auth-service 8080:80

# В другом терминале тестируем
curl http://localhost:8080/health/live
# Должен быть: {"status": "alive"}

curl http://localhost:8080/health/ready  
# Должен быть: {"status": "ready", "database": "connected"}

# Тестирование /api/register
curl -X POST http://localhost:8080/api/register ^
  -H "Content-Type: application/json" ^
  -d "{\"username\": \"student1\", \"password\": \"password123\"}"

curl -X POST http://localhost:8080/api/register ^
  -H "Content-Type: application/json" ^
  -d "{\"username\": \"student2\", \"password\": \"password456\", \"email\": \"student2@example.com\"}"

# Тестирование /api/login
curl -X POST http://localhost:8080/api/login ^
  -H "Content-Type: application/json" ^
  -d "{\"username\": \"student2\", \"password\": \"password456\"}"

# Тестирование /api/config
curl http://localhost:8080/api/config

# Тестирование /api/validate
# 1. Регистрация (если пользователя нет)
curl -X POST http://localhost:8080/api/register ^
  -H "Content-Type: application/json" ^
  -d "{\"username\": \"testuser\", \"password\": \"testpass\"}"

# 2. Логин и сохранение токена в переменную
curl -X POST http://localhost:8080/api/login ^
  -H "Content-Type: application/json" ^
  -d "{\"username\": \"testuser\", \"password\": \"testpass\"}" ^
  -o login_response.json

# 3. Извлекаем токен из файла (вручную)
type login_response.json
# замените YOUR_TOKEN
curl -H "Authorization: Bearer YOUR_TOKEN" http://localhost:8080/api/validate




















#===================================
# ЗАПУСК С ФРОНТЕНДОМ
#===================================
# Apply all Kubernetes configurations in order
kubectl apply -f kubernetes/namespace.yaml
kubectl apply -f kubernetes/secret.yaml
kubectl apply -f kubernetes/configmap-ui.yaml
kubectl apply -f kubernetes/configmap-postgres.yaml
kubectl apply -f kubernetes/postgres.yaml

# Wait for PostgreSQL to be ready
kubectl wait --for=condition=ready pod/postgres-0 -n user-platform-exam --timeout=300s

# Deploy auth-service
kubectl apply -f kubernetes/auth-service.yaml

# Wait for auth-service to be ready
kubectl wait --for=condition=ready pod -l app=auth-service -n user-platform-exam --timeout=300s

#----------------------------------
# DEPLOY auth-service
#----------------------------------
cd src/auth-service
docker build -t auth-service:latest .
docker images | findstr "auth-service"
cd ../..

# 8. Разворачиваем auth-service
kubectl apply -f kubernetes/auth-service.yaml


#----------------------------------
# DEPLOY frontend
#----------------------------------
# Build frontend Docker image
docker build -t frontend:latest frontend

# Deploy frontend
kubectl apply -f kubernetes/frontend.yaml

# Port-forward frontend (for local testing)
kubectl port-forward -n user-platform-exam service/frontend 8081:80


# Check all deployments
kubectl get all -n user-platform-exam

kubectl delete all --all
































#===================================
# ЗАПУСК НА 4
#===================================

cd ../..
D:
cd D:\Git\Microservices\LR_final__\LR_F

# Build all Docker images
docker build -t auth-service:latest src/auth-service
docker build -t profile-service:latest src/profile-service
docker build -t notification-service:latest src/notification-service
docker build -t frontend:latest frontend
docker build -t report-service:latest src/report-service

# Clean up existing deployments
kubectl delete all --all -n user-platform-exam

# Apply Kubernetes configurations in order
kubectl apply -f kubernetes/namespace.yaml
kubectl apply -f kubernetes/secret.yaml
kubectl apply -f kubernetes/configmap-ui.yaml
kubectl apply -f kubernetes/configmap-postgres.yaml

# Deploy PostgreSQL
kubectl apply -f kubernetes/postgres.yaml

# Wait for PostgreSQL to be ready
kubectl wait --for=condition=ready pod/postgres-0 -n user-platform-exam --timeout=300s

# Verify database tables
kubectl exec -n user-platform-exam postgres-0 -- psql -U admin -d userdb -c "\dt"

# Deploy all services
kubectl apply -f kubernetes/auth-service.yaml
kubectl apply -f kubernetes/profile-service.yaml
kubectl apply -f kubernetes/notification-service.yaml
kubectl apply -f kubernetes/report-service.yaml

# Wait for services to be ready
kubectl wait --for=condition=ready pod -l app=auth-service -n user-platform-exam --timeout=300s
kubectl wait --for=condition=ready pod -l app=profile-service -n user-platform-exam --timeout=300s
kubectl wait --for=condition=ready pod -l app=notification-service -n user-platform-exam --timeout=300s
kubectl wait --for=condition=ready pod -l app=report-service -n user-platform-exam --timeout=300s

# Deploy frontend
kubectl apply -f kubernetes/frontend.yaml

# Check all deployments
kubectl get all -n user-platform-exam

# Перевод фронта на порт для отображения
kubectl port-forward -n user-platform-exam service/frontend 8081:80

# Применение CronJob:
kubectl apply -f kubernetes/CronJob_daily-stats-collector.yaml
kubectl apply -f kubernetes/CronJob_notification-sender.yaml
kubectl apply -f kubernetes/CronJob_data-cleanup.yaml

# ------------------------------------------
# ПРОВЕРКА CronJob
# ------------------------------------------

# поменять интервал на каждыые 2 минуты CronJob_daily-stats-collector

# изменение расписания на тестовое
kubectl patch cronjob/daily-stats-collector -n user-platform-exam --type=merge -p "{\"spec\":{\"schedule\":\"*/1 * * * *\"}}"
kubectl patch cronjob/notification-sender -n user-platform-exam --type=merge -p "{\"spec\":{\"schedule\":\"*/1 * * * *\"}}"
kubectl patch cronjob/data-cleanup -n user-platform-exam --type=merge -p "{\"spec\":{\"schedule\":\"*/1 * * * *\"}}"

# отметить одно сообщение прочитаным и дождаться пока появится новое

# Проверка CronJob daily-stats-collector
kubectl exec -n user-platform-exam postgres-0 -- psql -U admin -d userdb -c "SELECT * FROM daily_user_stats;"

# возврат расписания
kubectl patch cronjob/daily-stats-collector -n user-platform-exam --type=merge -p "{\"spec\":{\"schedule\":\"*0 2 * * *\"}}"
kubectl patch cronjob/notification-sender -n user-platform-exam --type=merge -p "{\"spec\":{\"schedule\":\"*0 9 * * *\"}}"
kubectl patch cronjob/data-cleanup -n user-platform-exam --type=merge -p "{\"spec\":{\"schedule\":\"*0 0 * * 0\"}}"

# ------------------------------------------
# ДЛЯ ТЕСТИРОВНИЯ
# ------------------------------------------

# ЛОГИ СЕРВИСОВ
kubectl logs -n user-platform-exam -l app=auth-service -f
kubectl logs -n user-platform-exam -l app=profile-service -f
kubectl logs -n user-platform-exam -l app=notification-service -f
kubectl logs -n user-platform-exam -l app=frontend -f

# Перевод сервисов на порт для теста
kubectl port-forward -n user-platform-exam service/auth-service 8080:80
kubectl port-forward -n user-platform-exam service/profile-service 8082:80
kubectl port-forward -n user-platform-exam service/notification-service 8083:80
kubectl port-forward -n user-platform-exam service/report-service 8084:80

# ЛОГИ КРОНДЖОБОВ
kubectl get jobs -n user-platform-exam
kubectl logs job/daily-stats-collector-29464867 -n user-platform-exam

# Просмотр логов СКРИПТОМ
chmod +x scripts/view_cronjob_logs.sh
./scripts/view_cronjob_logs.sh

# Проверка содержимого таблицы notifications
kubectl exec -n user-platform-exam postgres-0 -- psql -U admin -d userdb -c "SELECT * FROM notifications;"

# Данные зарегистрированного аккаунта
student112
password123

# ------------------------------------------
# ОЧИСТКА
# ------------------------------------------
kubectl delete all --all -n user-platform-exam
kubectl get all -n user-platform-exam

docker image ls
docker image prune -a

docker ps -a
docker container prune













#===================================
# ЗАПУСК НА 5
#===================================

cd D:\Git\Microservices\LR_final__\LR_F

REM Собираем все Docker-образы локально
docker build -t auth-service:latest src/auth-service
docker build -t profile-service:latest src/profile-service
docker build -t notification-service:latest src/notification-service
docker build -t report-service:latest src/report-service
docker build -t frontend:latest frontend

REM Проверяем, что все образы собрались
docker images | findstr "auth-service\|profile-service\|notification-service\|report-service\|frontend"

REM Удаляем старый Helm release
helm uninstall user-platform-exam -n user-platform-exam

REM Удаляем старый namespace
kubectl delete namespace user-platform-exam

REM Ждём удаления
timeout /t 5 /nobreak

REM Устанавливаем Helm chart
helm install user-platform-exam ./helm --namespace user-platform-exam --create-namespace

REM Проверяем статус установки
helm status user-platform-exam -n user-platform-exam

REM Ждём, пока PostgreSQL запустится и инициализирует БД
kubectl wait --for=condition=ready pod/postgres-0 -n user-platform-exam --timeout=300s

REM Проверяем таблицы в БД
kubectl exec -n user-platform-exam postgres-0 -- psql -U admin -d userdb -c "\dt"

REM Проверяем, что таблица users пустая
kubectl exec -n user-platform-exam postgres-0 -- psql -U admin -d userdb -c "SELECT * FROM users;"

REM Ждём готовности auth-service
kubectl wait --for=condition=ready pod -l app=auth-service -n user-platform-exam --timeout=300s

REM Ждём готовности profile-service
kubectl wait --for=condition=ready pod -l app=profile-service -n user-platform-exam --timeout=300s

REM Ждём готовности notification-service
kubectl wait --for=condition=ready pod -l app=notification-service -n user-platform-exam --timeout=300s

REM Ждём готовности report-service
kubectl wait --for=condition=ready pod -l app=report-service -n user-platform-exam --timeout=300s

REM Ждём готовности frontend
kubectl wait --for=condition=ready pod -l app=frontend -n user-platform-exam --timeout=300s

REM Проверяем статус всех ресурсов
kubectl get all -n user-platform-exam

REM Проверяем, что CronJob'ы созданы
kubectl get cronjobs -n user-platform-exam

REM Смотрим расписание
kubectl describe cronjob/daily-stats-collector -n user-platform-exam
kubectl describe cronjob/notification-sender -n user-platform-exam
kubectl describe cronjob/data-cleanup -n user-platform-exam

REM Терминал: Frontend
kubectl port-forward -n user-platform-exam service/frontend 8081:80